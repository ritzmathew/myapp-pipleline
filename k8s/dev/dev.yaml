kind: Deployment
apiVersion: apps/v1
metadata:
  name: webgoat-dev
spec:
  replicas: 1
  selector:
      matchLabels:
          app: webgoat
          env: dev
  template:
    metadata:
      labels:
        app: webgoat
        env: dev
    spec:
      containers:
      - name: webgoat
        image: ritzmathew/webgoat:dev
        resources:
          limits:
            memory: "500Mi"
            cpu: "100m"
        imagePullPolicy: Always
        ports:
          containerPort: 443