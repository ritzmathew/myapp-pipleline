kind: Deployment
apiVersion: apps/v1
metadata:
  name: webgoat-prod
spec:
  replicas: 1
  selector:
      matchLabels:
          app: webgoat
          env: prod
  template:
    metadata:
      labels:
        app: webgoat
        env: prod
    spec:
      containers:
      - name: webgoat
        image: ritzmathew/webgoat:prod
        resources:
          limits:
            memory: "500Mi"
            cpu: "100m"
        imagePullPolicy: Always
        ports:
          containerPort: 443